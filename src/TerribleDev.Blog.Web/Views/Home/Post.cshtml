@model IPost
@{
    ViewData["Title"] = "Post";
}

<cache vary-by-route="postUrl">
    @Html.DisplayForModel()
</cache>

@section Head {
    <meta name="description" content="@Model.SummaryPlain" />
}
